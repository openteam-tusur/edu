<%= semantic_form_for resource, :as => resource_instance_name, :url => collection_url do |form| %>
  <%= form.error_messages %>
  <%= render :partial => 'form', :object => form %>
  <%= render :partial => 'buttons', :locals => { :form => form } %>
<% end %>
