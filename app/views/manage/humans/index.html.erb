<% unless @humans.empty? %>
  <ul>
  <% @humans.each do |human| %>
    <li><%= link_to human.full_name, manage_human_path(human) %></li>
  <% end %>
  </ul>
<% end %>
<p><%= link_to t("new"), new_manage_human_path, :class => "button" %></p>
