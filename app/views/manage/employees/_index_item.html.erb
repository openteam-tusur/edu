<div class="human">
  <p class="full_name"><%= item.full_name %></p>
  <p><%= link_to 'Редактировать', edit_manage_chair_employee_path(@chair, item), :class => 'button' %></p>
  <p>Должность: <%= item.accepted_employee_in_chair(@chair).post.mb_chars.capitalize %></p>
  <p><%= item.accepted_employee_in_chair(@chair).state %></p>
</div>

