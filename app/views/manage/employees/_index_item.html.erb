<div class="human">
  <p class="full_name"><%= item.full_name %></p>
  <p class="edit"><%= link_to 'Редактировать', edit_manage_chair_employee_path(@chair, item) %></p>
  <p class="post"><%= item.accepted_employee_in_chair(@chair).post.mb_chars.capitalize %></p>
  <p class="<%= item.accepted_employee_in_chair(@chair).state %>"><%= item.accepted_employee_in_chair(@chair).state %></p>
</div>

