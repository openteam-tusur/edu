<%= semantic_form_for resource, :as => :role, :url => manage_role_path do |form| %>
  <%= render :partial => "#{resource.class.to_s.underscore.gsub('roles/', '')}_fields", :locals => {:form => form} %>
  <%= form.buttons do %>
    <%= form.commit_button %>
  <% end %>
<% end %>

